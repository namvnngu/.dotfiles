version = 1

[envs.nodejs]
channels = ["conda-forge"]
dependencies = { nodejs = "*" }
exposed = { corepack = "corepack", node = "node", npm = "npm", npx = "npx", pnpm = "pnpm" }

[envs.typescript-language-server]
channels = ["conda-forge"]
dependencies = { typescript-language-server = "*" }
exposed = { typescript-language-server = "typescript-language-server" }

[envs.prettier]
channels = ["conda-forge"]
dependencies = { prettier = "*" }
exposed = { prettier = "prettier" }

[envs.biome]
channels = ["conda-forge"]
dependencies = { biome = "*" }
exposed = { biome = "biome" }

[envs.typos-lsp]
channels = ["conda-forge"]
dependencies = { typos-lsp = "*" }
exposed = { typos-lsp = "typos-lsp" }

[envs.stylua]
channels = ["conda-forge"]
dependencies = { stylua = "*" }
exposed = { stylua = "stylua" }

[envs.clang-format]
channels = ["conda-forge"]
dependencies = { clang-format = "*" }
exposed = { clang-format = "clang-format" }

[envs.lua-language-server]
channels = ["conda-forge"]
dependencies = { lua-language-server = "*" }
exposed = { lua-language-server = "lua-language-server" }

[envs.jq]
channels = ["conda-forge"]
dependencies = { jq = "*" }
exposed = { jq = "jq" }

[envs.shellcheck]
channels = ["conda-forge"]
dependencies = { shellcheck = "*" }
exposed = { shellcheck = "shellcheck" }

[envs.go]
channels = ["conda-forge"]
dependencies = { go = "*" }
exposed = { go = "go", gofmt = "gofmt" }

[envs.gopls]
channels = ["conda-forge"]
dependencies = { gopls = "*" }
exposed = { gopls = "gopls" }

[envs.typst]
channels = ["conda-forge"]
dependencies = { typst = "*" }
exposed = { typst = "typst" }

[envs.tinymist]
channels = ["conda-forge"]
dependencies = { tinymist = "*" }
exposed = { tinymist = "tinymist" }
