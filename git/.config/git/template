

# <type>(<scope>): <short summary>
# 
# <short summary> is in present tense, not capitalized and has no period at the end.
# <scope> is optional.
# <type> is
#     feat:     a new feature
#     fix:      a bug fix
#     docs:     documentation changes
#     style:    code style changes (e.g. whitespaces, formatting, missing semicolons)
#     refactor: code changes that neither fix a bug nor add a feature (e.g. renaming a variable)
#     test:     test changes that add missing tests or corrects existing tests
#     ci:       changes that affect ci configuration files or pipelines
#     build:    changes that affect the build system or external dependencies
#     git:      git actions (e.g. git pull, git merge)

